# ── Database ────────────────────────────────────────────────────────────────
# Supabase PostgreSQL — use the pooler URL (port 6543) for runtime queries
DATABASE_URL=
# Direct connection for prisma db push / migrations (port 5432)
DIRECT_URL=

# ── File storage ─────────────────────────────────────────────────────────────
# Vercel Blob — primary storage backend (uploads, documents, images).
# Create a Blob store in the Vercel dashboard, then paste its read/write token
# here (or run `vercel env pull` to fetch it). When set, all uploads go to Blob.
BLOB_STORE_ID=
BLOB_READ_WRITE_TOKEN=

# ── AI (blog post generation) ────────────────────────────────────────────────
# Anthropic API key — required for the "Generate blog post" button.
ANTHROPIC_API_KEY=
# Optional model override (defaults to claude-sonnet-4-6)
ANTHROPIC_MODEL=claude-sonnet-4-6

# ── SmartBid (ConstructConnect) tender sync ──
# The public-projects endpoint just needs the base URL + ClientKey (no header auth).
SMARTBID_API_BASE=
SMARTBID_CLIENT_KEY=
# Optional:
# SMARTBID_PROJECTS_PATH=/project/publicProjects
# SMARTBID_OFFICE_KEY=
# SMARTBID_STATUSES=OpenToBid,Upcoming
# SMARTBID_PROJECT_URL_TEMPLATE=https://securecc.smartbidnet.com/External/PublicProject.aspx?Id={link}
#
# Header auth — only for other, account-protected endpoints (not public projects):
# SMARTBID_AUTH=apikey        # "apikey" | "passport" | "oauth"
# SMARTBID_API_KEY=
# SMARTBID_AUTH_HEADER=Authorization
# SMARTBID_AUTH_SCHEME=Bearer
# SMARTBID_PASSPORT_KEY=       # session token from a SmartBid login (expires on logout)
# SMARTBID_TOKEN_URL=
# SMARTBID_CLIENT_ID=
# SMARTBID_CLIENT_SECRET=
# Pegah's SmartBid subcontractor-registration form (embedded on the page):
SMARTBID_SUBCONTRACTOR_FORM_URL=
